<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.binance.trade.mapper.TradeRsiMapper">
    <select id="selectTradeRsi" resultType="com.binance.trade.model.TradeRsi" parameterType="String">
        SELECT /* selectTradeRsi */
               avg(price) as price
             , dateYmd
        FROM (
                 SELECT
                   price
                 , LEFT (from_unixtime(LEFT (`time`, 10)), 18) AS `dateYmd`
                 FROM
                    trade_history_${value}) T
        GROUP BY dateYmd
        ORDER BY dateYmd DESC LIMIT 7
    </select>
</mapper>